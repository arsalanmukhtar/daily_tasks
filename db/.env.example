# Copy to .env - points migrate-from-firestore.js at the target database.
# For rehearsal, point this at a throwaway database; for the real cutover,
# use the same values as server/.env.
DB_HOST=localhost
DB_PORT=5432
DB_NAME=daily_tasks
DB_USER=daily_tasks_app
DB_PASSWORD=
