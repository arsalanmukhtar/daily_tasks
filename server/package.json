{
  "name": "daily-tasks-server",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "REST + WebSocket API backing the daily_tasks web and Flutter apps - replaces Firebase (Firestore/Auth) and push-daemon. See PROJECT.md and db/schema.sql.",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^5.1.0",
    "jsonwebtoken": "^9.0.2",
    "multer": "^2.0.1",
    "nodemailer": "^10.0.1",
    "pg": "^8.16.3",
    "ws": "^8.18.0"
  }
}
