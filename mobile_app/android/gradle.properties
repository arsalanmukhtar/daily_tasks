org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
# Windows-only: Kotlin's incremental compiler cache (memory-mapped file
# storage) intermittently fails to close cleanly on this filesystem,
# failing otherwise-successful compiles with a spurious
# "Could not close incremental caches" error. Disabling incremental
# compilation trades a little build speed for reliability.
kotlin.incremental=false
