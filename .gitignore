# Local tooling / IDE
.claude/
.vscode/
.idea/

# OS detritus
.DS_Store
Thumbs.db

# Logs
*.log

# Local-only theme picker helper (not part of the app)
theme-preview.html

# One-time data-seed scripts (run once in Apps Script, not part of the app)
apps-script/seed-mustafa-haider.gs
apps-script/seed-arsalan-mukhtar.gs

# Secrets and build artifacts - never commit these
*firebase-adminsdk*.json
*.jks
*.keystore
keystore_password*.txt
*.apk
*.aab
Leave Approvals*.zip

# Local Android signing workspace (keystore, signing tool, build output) -
# not project source, stays local only
/android_signing/
